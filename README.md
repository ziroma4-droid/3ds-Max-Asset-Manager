# 3ds Max Asset Manager

Программа для управления текстурами и прокси в сценах 3ds Max. Сохраняет только используемые ресурсы в единую папку `maps`. Неиспользуемые ресурсы переносятся в папку `unused`.

## Установка

1. Убедитесь, что установлен Python 3.10 или выше
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

### Быстрый запуск (Windows)
- Двойной клик на `start.bat` или `start.ps1`

### Ручной запуск
```bash
# Активируйте виртуальное окружение
venv\Scripts\Activate.ps1  # PowerShell
# или
venv\Scripts\activate.bat  # CMD

# Запустите приложение
python main.py
```

### Запуск без консоли
```bash
venv\Scripts\pythonw.exe main.py
```

## Использование

1. Выберите файл сцены `.max` или папку со сценами
2. Нажмите "🔍 Анализировать" для сканирования ассетов
3. Нажмите "📦 Организовать файлы" для перемещения файлов в `maps` и `unused`
4. Сохраните отчет при необходимости

## Возможности

- ✅ Анализ одной сцены или папки со сценами
- ✅ Поиск всех текстур, прокси и других ассетов
- ✅ Автоматическая организация файлов в папку `maps`
- ✅ Удаление дубликатов файлов
- ✅ Перемещение неиспользуемых файлов в `unused`
- ✅ Детальная статистика по папкам
- ✅ Экспорт отчета
